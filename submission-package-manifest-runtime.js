/* ANTENEH RESEARCH HUB — submission package manifest */
(()=>{
 const read=()=>JSON.parse(localStorage.getItem('arlab')||'{}');
 const write=s=>localStorage.setItem('arlab',JSON.stringify(s));
 const esc=x=>String(x??'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c]));
 const hash=x=>{let h=2166136261;for(const c of String(x))h=Math.imul(h^c.charCodeAt(0),16777619);return (h>>>0).toString(16)};
 const present=x=>x!==undefined&&x!==null&&String(x).trim()!=='';
 const journalReqStatus=p=>{const r=p.journalRequirementsVerification||{};if(!r.sourceUrl||!r.journal||p.journal!==r.journal)return'missing';const items=Object.values(r.items||{});if(!items.length)return'unverified';const payload={journal:r.journal,sourceUrl:r.sourceUrl,sourceTitle:r.sourceTitle||'',verifiedOn:r.verifiedOn||'',sourceNotes:r.sourceNotes||'',items:r.items};if(hash(JSON.stringify(payload))!==r.fingerprint)return'unverified';if(items.some(x=>x.status==='missing'))return'missing';if(items.some(x=>x.status==='unverified'))return'unverified';return'verified'};
 const build=s=>{const p=s.publicationPackage||{},c=p.submissionChecklist||{},refs=s.referenceLibrary||[],jr=p.journalRequirementsVerification||{},jrStatus=journalReqStatus(p);const files=[];const add=(id,label,status,detail='')=>files.push({id,label,status,detail});add('manuscript','Main manuscript',present(s.manuscript?.content||s.manuscript?.text||s.manuscriptReadinessApproved)?'ready':'missing');add('references','Reference library',Array.isArray(refs)&&refs.length?'ready':'missing',Array.isArray(refs)?`${refs.length} records`:'No reference library');add('reporting-checklist','Reporting checklist',c.reportingChecklist||p.reportingGuideline?'ready':'unverified');add('journal-requirements','Journal requirements verification',jrStatus,p.journalRequirementsVerification?.sourceUrl||'Official Instructions for Authors URL required');add('submission-plan','Journal submission plan',present(p.submissionPlanFingerprint)?'ready':'missing','Plan must be saved only after valid journal requirements verification.');add('cover-letter','Cover letter',present(p.coverLetterNotes||c.coverLetter)?'ready':'missing');add('ethics','Ethics statement',c.ethics==='not_applicable'||c.ethics==='verified'?'ready':present(c.ethics)?'unverified':'missing');add('consent','Consent statement',c.informedConsent==='not_applicable'||c.informedConsent==='verified'?'ready':present(c.informedConsent)?'unverified':'missing');add('funding','Funding statement',c.funding==='not_applicable'||c.funding==='verified'?'ready':present(c.funding)?'unverified':'missing');add('conflict','Conflict of interest',c.conflictOfInterest==='not_applicable'||c.conflictOfInterest==='verified'?'ready':present(c.conflictOfInterest)?'unverified':'missing');add('data-availability','Data availability',c.dataAvailability==='not_applicable'||c.dataAvailability==='verified'?'ready':present(c.dataAvailability)?'unverified':'missing');add('registration','Registration / protocol',c.registration==='not_applicable'||c.registration==='verified'?'ready':present(c.registration)?'unverified':'missing');add('supplementary','Supplementary files',c.supplementaryFiles==='not_applicable'||c.supplementaryFiles==='verified'?'ready':present(c.supplementaryFiles)?'unverified':'missing');const blocking=files.filter(x=>x.status!=='ready');const manifest={createdAt:new Date().toISOString(),journal:p.journal||'',articleType:p.articleType||'',authors:p.authors||p.authorNames||'',submissionPlanFingerprint:p.submissionPlanFingerprint||'',journalRequirementsFingerprint:jr.fingerprint||'',journalRequirementsStatus:jrStatus,files,blockingCount:blocking.length,packageFingerprint:hash(JSON.stringify({journal:p.journal,articleType:p.articleType,authors:p.authors||p.authorNames,submissionPlanFingerprint:p.submissionPlanFingerprint||'',journalRequirementsFingerprint:jr.fingerprint||'',files}))};return manifest};
 const render=stage=>{if(Number(stage)!==21)return;const root=document.getElementById('content');if(!root)return;document.getElementById('submissionPackageManifestRuntime')?.remove();const s=read(),m=build(s),box=document.createElement('div');box.id='submissionPackageManifestRuntime';box.className='card';box.innerHTML=`<h4>Submission Package Manifest</h4><p>A final inventory of the files and declarations that should travel with the manuscript. It does not claim a file exists unless the research record supports it.</p><div class="paper"><b>Journal:</b> ${esc(m.journal||'Not specified')}<br><b>Article type:</b> ${esc(m.articleType||'Not specified')}<br><b>Blocking items:</b> ${m.blockingCount}<br><b>Journal requirements status:</b> ${esc(m.journalRequirementsStatus)}<br><b>Submission plan fingerprint:</b> <code>${esc(m.submissionPlanFingerprint||'Not recorded')}</code><br><b>Journal requirements fingerprint:</b> <code>${esc(m.journalRequirementsFingerprint||'Not recorded')}</code><br><b>Package fingerprint:</b> <code>${m.packageFingerprint}</code></div><div>${m.files.map(f=>`<div class="paper"><b>${esc(f.label)}</b> — ${esc(f.status)}${f.detail?`<br><small>${esc(f.detail)}</small>`:''}</div>`).join('')}</div><div class="actions"><button class="btn primary" id="spmSave">Save manifest</button><button class="btn" id="spmExport">Export manifest JSON</button></div><div id="spmStatus"></div>`;root.appendChild(box);document.getElementById('spmSave').onclick=()=>{const x=read();x.publicationPackage={...(x.publicationPackage||{}),submissionManifest:m};write(x);document.getElementById('spmStatus').innerHTML='<p class="notice"><b>Manifest saved.</b> The package fingerprint is now part of the research record.</p>'};document.getElementById('spmExport').onclick=()=>{const u=URL.createObjectURL(new Blob([JSON.stringify(m,null,2)],{type:'application/json'})),d=document.createElement('a');d.href=u;d.download='anteneh-submission-package-manifest.json';d.click();setTimeout(()=>URL.revokeObjectURL(u),1000)}};
 window.SubmissionPackageManifestRuntime={render,build};
})();
