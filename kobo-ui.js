/* ANTENEH RESEARCH HUB — secure KoboToolbox client bridge */
(()=>{
 const R=()=>JSON.parse(localStorage.getItem('arlab')||'{}'),W=x=>localStorage.setItem('arlab',JSON.stringify(x));
 const E=x=>String(x??'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c]));
 const add=h=>{const c=document.getElementById('content');if(c&&!document.getElementById('kobo-card'))c.insertAdjacentHTML('afterbegin',h)};
 const validate=(h,r)=>{const e=[],w=[];if(!h.length)e.push('Kobo dataset contains no columns.');if(h.some(x=>!String(x).trim()))e.push('Kobo dataset contains an unnamed column.');if(new Set(h.map(x=>String(x).trim().toLowerCase())).size!==h.length)e.push('Kobo dataset contains duplicate column names.');if(!r.length)e.push('Kobo project contains no submission records.');if(r.some(x=>x.length!==h.length))e.push('Kobo records have inconsistent column counts.');if(r.length>100000)w.push('Large dataset: browser memory limits may affect analysis.');return{errors:e,warnings:w}};
 const dictionary=(h,r)=>h.map((name,j)=>{const raw=r.map(x=>String(x[j]??'').trim()),p=raw.filter(Boolean),numeric=p.length>0&&p.every(x=>Number.isFinite(Number(x)));return{name,role:numeric?'numeric':'categorical',rows:r.length,missing:r.length-p.length,missingPct:r.length?Math.round((r.length-p.length)/r.length*1000)/10:0,unique:new Set(p).size,numeric}});
 const hash=async(ds)=>{const raw=JSON.stringify({assetUid:ds.assetUid,headers:ds.headers,data:ds.data}),bytes=new TextEncoder().encode(raw);if(crypto?.subtle){const d=await crypto.subtle.digest('SHA-256',bytes);return Array.from(new Uint8Array(d)).map(b=>b.toString(16).padStart(2,'0')).join('')}return `local-${ds.recordCount||0}-${ds.headers?.length||0}`};
 function render(stage){if(stage!==10&&stage!==11&&stage!==12)return;const s=R();add(`<div class="card" id="kobo-card"><h4>KoboToolbox → Research Hub</h4><p>Connect a Kobo project by its asset UID. The API token stays server-side and is never entered into this browser.</p><div class="field"><label>KOBO PROJECT ASSET UID</label><input id="koboUid" placeholder="e.g. a4etXeWtqcoodSxLV8a6Uq" autocomplete="off"></div><button class="btn primary" id="koboLoad">Fetch Kobo data</button><div class="notice" id="koboStatus" style="margin-top:10px">Ready. Dataset analysis remains blocked until the quality gate passes.</div></div>`);const input=document.getElementById('koboUid'),btn=document.getElementById('koboLoad'),status=document.getElementById('koboStatus');if(!input||!btn||!status)return;input.value=s.kobo?.assetUid||'';btn.onclick=async()=>{const assetUid=input.value.trim();if(!/^[A-Za-z0-9_-]+$/.test(assetUid)){status.innerHTML='<b>Invalid asset UID.</b> Enter the Kobo project UID only.';return}btn.disabled=true;status.textContent='Fetching Kobo submissions securely…';try{const r=await fetch('/api/kobo',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({assetUid})}),d=await r.json().catch(()=>({}));if(!r.ok||!d.ok)throw new Error(d.error||'Kobo ingestion failed.');const ds=d.dataset||{},h=Array.isArray(ds.headers)?ds.headers:[],rows=Array.isArray(ds.data)?ds.data:[],v=validate(h,rows),now=new Date().toISOString(),next=R();next.kobo={assetUid:d.assetUid,fetchedAt:d.fetchedAt,recordCount:d.recordCount,pages:d.pages,connected:true,lastSync:now};if(v.errors.length){next.dataset=null;W(next);status.innerHTML='<b>Dataset blocked.</b><br>'+v.errors.map(E).join('<br>');return}const dict=dictionary(h,rows),dataset={...ds,dataDictionary:dict,quality:{validated:true,warnings:v.warnings,validatedAt:now},provenance:{source:'KoboToolbox',assetUid:d.assetUid,fetchedAt:d.fetchedAt,recordCount:d.recordCount,pages:d.pages,ingestedAt:now}};dataset.provenance.sha256=await hash(dataset);next.dataset=dataset;next.koboHistory=Array.isArray(next.koboHistory)?next.koboHistory:[];next.koboHistory.push({assetUid:d.assetUid,fetchedAt:d.fetchedAt,ingestedAt:now,recordCount:d.recordCount,pages:d.pages,sha256:dataset.provenance.sha256});next.koboHistory=next.koboHistory.slice(-20);W(next);status.innerHTML=`<b>✓ Kobo data loaded.</b><br>${E(d.recordCount)} records · ${E(h.length)} columns · ${E(d.pages)} page(s)<br><span class="muted">Quality gate passed. Statistical execution still requires the approved analysis plan.</span>`;const dq=document.getElementById('dq');if(dq)dq.innerHTML='<b>Data dictionary</b><br>'+dict.map(x=>E(x.name)+' — '+x.role+' · missing '+x.missingPct+'% · '+x.unique+' unique').join('<br>')+(v.warnings.length?'<br><br><b>Warnings</b><br>'+v.warnings.map(E).join('<br>'):'')+'<br><br><b>Dataset fingerprint</b><br><small>'+E(dataset.provenance.sha256)+'</small>';window.AnalysisEngine?.render?.();window.ResultsEngine?.render?.();window.InferentialEngine?.render?.();window.WebREngine?.render?.();window.RAnalysisEngine?.render?.()}catch(e){status.innerHTML='<b>Kobo connection failed:</b> '+E(e.message||'Unknown error')}finally{btn.disabled=false}}}
 window.KoboUI={render};
})();
