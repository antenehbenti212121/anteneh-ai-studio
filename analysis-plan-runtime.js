/* ANTENEH RESEARCH HUB — executable statistical analysis plan gate */
(()=>{
 const R=()=>JSON.parse(localStorage.getItem('arlab')||'{}');
 const W=x=>localStorage.setItem('arlab',JSON.stringify(x));
 const E=x=>String(x??'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c]));
 const add=h=>{const c=document.getElementById('content');if(c&&!document.getElementById('analysis-plan-runtime'))c.insertAdjacentHTML('afterbegin',h)};
 const hash=s=>{let h=2166136261;for(let i=0;i<s.length;i++){h^=s.charCodeAt(i);h=Math.imul(h,16777619)}return ('00000000'+(h>>>0).toString(16)).slice(-8)};
 const canonical=s=>String(s||'').trim().toLowerCase();
 const render=stage=>{
  if(stage!==13)return;
  const s=R(),d=s.dataset||{},old=s.analysisPlan||{};
  const headers=Array.isArray(d.headers)?d.headers:[];
  add(`<div class="card" id="analysis-plan-runtime"><h4>🧭 Executable Statistical Analysis Plan</h4><p>Define exactly what may be executed before R runs. The Hub records the dataset identity, analysis specification, approval state and generated code version.</p><div class="field"><label>PRIMARY OUTCOME / DEPENDENT VARIABLE</label><select id="aprOutcome"><option value="">Select variable…</option>${headers.map(x=>`<option value="${E(x)}">${E(x)}</option>`).join('')}</select></div><div class="field"><label>PRIMARY EXPOSURE / PREDICTOR</label><select id="aprExposure"><option value="">Select variable…</option>${headers.map(x=>`<option value="${E(x)}">${E(x)}</option>`).join('')}</select></div><div class="field"><label>PRIMARY ANALYSIS / TEST</label><select id="aprModel"><option value="">Select…</option><option>Descriptive statistics</option><option>One-sample t-test</option><option>Independent two-sample t-test</option><option>Paired t-test</option><option>Chi-square test</option><option>Pearson correlation</option><option>Linear regression</option><option>Logistic regression</option><option>Other — specify in notes</option></select></div><div class="field"><label>MISSING-DATA STRATEGY</label><select id="aprMissing"><option value="">Select…</option><option>Complete-case analysis</option><option>Report missingness; no imputation</option><option>Multiple imputation — requires protocol</option><option>Other — specify in notes</option></select></div><div class="field"><label>ASSUMPTIONS / DIAGNOSTICS</label><textarea id="aprAssumptions" rows="3" placeholder="e.g. distribution/linearity, independence, homoscedasticity, multicollinearity, expected cell counts..."></textarea></div><div class="field"><label>ADDITIONAL NOTES / SUBGROUPS / MULTIPLE-COMPARISON PLAN</label><textarea id="aprNotes" rows="3" placeholder="Pre-specified subgroup analyses, adjustment plan, effect sizes, confidence intervals, multiplicity... "></textarea></div><label style="display:flex;gap:8px;align-items:center;color:#b7c8da;font-size:12px"><input type="checkbox" id="aprApprove"> I have reviewed and approve this statistical analysis plan.</label><div class="actions"><button class="btn primary" id="aprSave">Save analysis plan</button><button class="btn" id="aprCode">Generate exact R specification</button></div><div id="aprStatus" class="notice" style="margin-top:10px"></div><textarea id="aprR" style="min-height:230px;margin-top:10px;display:none" readonly></textarea></div>`);
  const set=(id,v)=>{const el=document.getElementById(id);if(el)el.value=v||''};
  set('aprOutcome',old.outcome);set('aprExposure',old.exposure);set('aprModel',old.model||old.analysis);set('aprMissing',old.missing);set('aprAssumptions',old.assumptions);set('aprNotes',old.notes);document.getElementById('aprApprove').checked=!!old.approved;
  const spec=plan=>`# ANTENEH RESEARCH HUB — approved analysis specification\n# Plan fingerprint: ${hash(JSON.stringify(plan))}\n# Dataset: ${d.name||'not specified'} | rows=${d.rows||0} | columns=${d.columns||0}\n# Execute only after researcher approval.\n\ndata <- read.csv(${JSON.stringify((d.name||'data.csv').replace(/[^A-Za-z0-9_.-]/g,'_'))}, stringsAsFactors=FALSE, check.names=FALSE)\n\n# Outcome: ${plan.outcome||'NOT SPECIFIED'}\n# Exposure/predictor: ${plan.exposure||'NOT SPECIFIED'}\n# Primary analysis: ${plan.model||'NOT SPECIFIED'}\n# Missing-data strategy: ${plan.missing||'NOT SPECIFIED'}\n# Assumptions/diagnostics: ${plan.assumptions||'NOT SPECIFIED'}\n# Notes: ${plan.notes||'NONE'}\n\nstr(data)\nsummary(data)\ncolSums(is.na(data))\n\n# Add the exact approved inferential procedure here; report effect estimates, 95% CIs and p-values where appropriate.\n# Do not change the analysis without recording a new plan version and re-approval.\n`;
  const status=document.getElementById('aprStatus');
  const collect=()=>({outcome:document.getElementById('aprOutcome').value.trim(),exposure:document.getElementById('aprExposure').value.trim(),model:document.getElementById('aprModel').value.trim(),missing:document.getElementById('aprMissing').value.trim(),assumptions:document.getElementById('aprAssumptions').value.trim(),notes:document.getElementById('aprNotes').value.trim(),approved:document.getElementById('aprApprove').checked});
  document.getElementById('aprSave').onclick=()=>{const p=collect();const missing=['outcome','model','missing'].filter(k=>!p[k]);if(missing.length){status.innerHTML=`<b>Cannot save:</b> ${missing.join(', ')} must be specified.`;return}const now=new Date().toISOString(),plan={...p,updatedAt:now,version:hash(JSON.stringify(p)+now)};const n=R();n.analysisPlan=plan;n.analysisPlanApproved=!!p.approved;n.analysisPlanApprovedAt=p.approved?now:null;n.analysisPlanFingerprint=hash(JSON.stringify(plan));n.analysisPlanRSpecification=spec(plan);W(n);status.innerHTML=p.approved?`<b>APPROVED.</b> Plan version ${E(plan.version)} is now the release contract for statistical execution.`:`<b>Saved as DRAFT.</b> Statistical execution remains locked until approval.`};
  document.getElementById('aprCode').onclick=()=>{const p=collect(),r=spec(p);const box=document.getElementById('aprR');box.value=r;box.style.display='block';try{navigator.clipboard?.writeText(r)}catch(_){} };
  status.innerHTML=old.approved?`Current plan: <b>APPROVED</b> · version ${E(old.version||'legacy')}`:'Current plan: <b>DRAFT / NOT APPROVED</b>.';
 };
 window.AnalysisPlanRuntime={render};
 if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>render(Number(R().current||0)));else render(Number(R().current||0));
 setInterval(()=>render(Number(R().current||0)),1800);
})();
