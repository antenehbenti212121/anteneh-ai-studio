/* ANTENEH RESEARCH HUB — journal submission preparation runtime */
(()=>{
 const read=()=>JSON.parse(localStorage.getItem('arlab')||'{}');
 const write=s=>localStorage.setItem('arlab',JSON.stringify(s));
 const esc=x=>String(x??'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c]));
 const hash=x=>{let h=2166136261;for(const c of String(x))h=Math.imul(h^c.charCodeAt(0),16777619);return (h>>>0).toString(16)};
 const guidelineLinks={STROBE:'https://www.equator-network.org/reporting-guidelines/strobe/',CONSORT:'https://www.equator-network.org/reporting-guidelines/consort/',PRISMA:'https://www.equator-network.org/reporting-guidelines/prisma/', 'SPIRIT / PRISMA-P':'https://www.equator-network.org/reporting-guidelines/spirit-2013-statement-defining-standard-protocol-items-for-clinical-trials/',STARD:'https://www.equator-network.org/reporting-guidelines/stard/',TRIPOD:'https://www.equator-network.org/reporting-guidelines/tripod-statement/',CARE:'https://www.equator-network.org/reporting-guidelines/care/', 'SRQR / COREQ':'https://www.equator-network.org/reporting-guidelines/srqr/',SQUIRE:'https://www.equator-network.org/reporting-guidelines/squire-2-0/',CHEERS:'https://www.equator-network.org/reporting-guidelines/cheers/','AGREE / RIGHT':'https://www.equator-network.org/reporting-guidelines/right-statement/'};
 const journalVerification=p=>{const r=p.journalRequirementsVerification||{},items=Object.values(r.items||{}),payload={journal:r.journal||'',sourceUrl:r.sourceUrl||'',sourceTitle:r.sourceTitle||'',verifiedOn:r.verifiedOn||'',sourceNotes:r.sourceNotes||'',items:r.items||{}};const fingerprint=hash(JSON.stringify(payload));const counts={verified:0,missing:0,unverified:0,not_applicable:0};items.forEach(x=>{if(counts[x.status]!==undefined)counts[x.status]++});const valid=!!r.journal&&!!r.sourceUrl&&!!items.length&&fingerprint===r.fingerprint&&p.journal===r.journal&&!items.some(x=>x.status==='missing'||x.status==='unverified');return{record:r,items,counts,fingerprint,valid};};
 const render=stage=>{if(Number(stage)!==21)return;const root=document.getElementById('content');if(!root)return;document.getElementById('journalSubmissionRuntime')?.remove();const s=read(),pkg=s.publicationPackage||{},guideline=pkg.reportingGuideline||'Custom / verify with EQUATOR',jr=journalVerification(pkg);const box=document.createElement('div');box.id='journalSubmissionRuntime';box.className='card';box.innerHTML=`<h4>Journal submission preparation</h4><p>Submission requirements are journal-specific. Requirements are verified in the Journal Requirements Verification record; this layer does not create a second, unverified requirement list.</p><div class="paper"><b>Journal requirements verification:</b> <span>${jr.valid?'VERIFIED':'BLOCKED — VERIFY JOURNAL REQUIREMENTS FIRST'}</span><br><small>Verified: ${jr.counts.verified} · Missing: ${jr.counts.missing} · Unverified: ${jr.counts.unverified} · N/A: ${jr.counts.not_applicable}</small>${jr.record.sourceUrl?`<br><a href="${esc(jr.record.sourceUrl)}" target="_blank" rel="noopener">Open recorded official source</a>`:''}</div><div class="paper"><b>Reporting guideline:</b> ${esc(guideline)}<br>${guidelineLinks[guideline]?`<a href="${guidelineLinks[guideline]}" target="_blank" rel="noopener">Open guideline source</a>`:'Select/verify the appropriate guideline in EQUATOR.'}</div><div class="field"><label>JOURNAL / VENUE</label><input id="jsJournal" value="${esc(pkg.journal||'')}" placeholder="Enter target journal or venue"></div><div class="field"><label>ARTICLE TYPE</label><input id="jsType" value="${esc(pkg.articleType||'Original Research')}" placeholder="Original Research, Brief Report, Protocol, etc."></div><div class="field"><label>AUTHOR NAMES / ORDER</label><textarea id="jsAuthors" placeholder="Author 1; Author 2; Corresponding author..."></textarea></div><div class="field"><label>COVER LETTER NOTES</label><textarea id="jsCover" placeholder="Study significance, novelty, fit, ethics/approval, originality, conflicts, funding, data availability."></textarea></div><div class="actions"><button class="btn primary" id="jsSave">Save submission plan</button><button class="btn" id="jsCoverExport">Export cover-letter draft</button><button class="btn" id="jsJSON">Export submission plan JSON</button></div><div id="jsStatus"></div>`;root.prepend(box);jsAuthors.value=pkg.authors||'';jsCover.value=pkg.coverLetterNotes||'';jsSave.onclick=()=>{const journal=jsJournal.value.trim(),articleType=jsType.value.trim(),authors=jsAuthors.value.trim(),coverLetterNotes=jsCover.value.trim();if(!journal){jsStatus.innerHTML='<p class="notice"><b>Save blocked.</b> Enter the actual target journal or venue.</p>';return}const x=read(),v=journalVerification(x.publicationPackage||{});if(!v.valid||journal!==v.record.journal){jsStatus.innerHTML='<p class="notice"><b>Save blocked.</b> The submission plan must match a valid, fingerprinted Journal Requirements Verification record. Complete or refresh Stage 21 journal verification first.</p>';return}x.publicationPackage={...(x.publicationPackage||{}),journal,articleType,authors,coverLetterNotes,submissionPlanSavedAt:new Date().toISOString(),submissionPlanFingerprint:hash(JSON.stringify({journal,articleType,authors,journalRequirementsFingerprint:v.fingerprint,coverLetterNotes}))};write(x);jsStatus.innerHTML=`<p class="notice"><b>Submission plan saved.</b> Linked to journal-requirements fingerprint ${esc(v.fingerprint)}. Nothing has been submitted to a journal.</p>`};jsCoverExport.onclick=()=>{const x=read(),p=x.publicationPackage||{},v=journalVerification(p);const text=`COVER LETTER DRAFT\n\nDear Editor,\n\nPlease consider the manuscript “${x.title||'[Manuscript title]'}” for ${p.journal||'[Target journal]'}.\n\nThis manuscript reports ${p.articleType||'original research'} and has been prepared with study-design-appropriate reporting guidance (${p.reportingGuideline||'to be verified'}) and internal research-quality controls.\n\nKey significance / novelty:\n${p.coverLetterNotes||'[Researcher to complete]'}\n\nAuthors:\n${p.authors||'[Researcher to complete]'}\n\nJournal requirements verification status: ${v.valid?'verified':'not verified'}\nJournal requirements fingerprint: ${v.fingerprint||'[none]'}\n\nSincerely,\n${(p.authors||'[Corresponding author]').split(';')[0].trim()}`;const u=URL.createObjectURL(new Blob([text],{type:'text/plain;charset=utf-8'})),a=document.createElement('a');a.href=u;a.download='anteneh-cover-letter-draft.txt';a.click();setTimeout(()=>URL.revokeObjectURL(u),1000)};jsJSON.onclick=()=>{const x=read(),p=x.publicationPackage||{},v=journalVerification(p);const out={exportedAt:new Date().toISOString(),journal:p.journal||'',articleType:p.articleType||'',authors:p.authors||'',reportingGuideline:p.reportingGuideline||'',coverLetterNotes:p.coverLetterNotes||'',journalRequirementsVerification:{valid:v.valid,fingerprint:v.fingerprint,sourceUrl:v.record.sourceUrl||'',counts:v.counts}};const u=URL.createObjectURL(new Blob([JSON.stringify(out,null,2)],{type:'application/json'})),a=document.createElement('a');a.href=u;a.download='anteneh-journal-submission-plan.json';a.click();setTimeout(()=>URL.revokeObjectURL(u),1000)}};
 window.JournalSubmissionRuntime={render,journalVerification};
})();
